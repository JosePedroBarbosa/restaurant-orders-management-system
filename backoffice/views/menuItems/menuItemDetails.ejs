<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= menuItem.name %> - FlavorFlow</title>
    <link rel="icon" type="image/x-icon" href="/images/icon/icon.png">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.0/font/bootstrap-icons.css"
    />
    <style>
      .btn-primary {
        background-color: var(--primary-dark);
        border-color: var(--primary-dark);
        padding: 8px 20px;
        border-radius: 25px;
        font-weight: 600;
        transition: all 0.3s ease;
      }

      .btn-primary:hover {
        background-color: var(--accent-color);
        border-color: var(--accent-color);
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(255, 69, 0, 0.2);
      }

      .item-header {
        position: relative;
        overflow: hidden;
        border-radius: 15px;
        margin-bottom: 30px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }

      .item-info-header {
        position: relative;
        background-color: var(--primary-color);
        padding: 20px;
        border-radius: 15px 15px 0 0;
        color: white;
        text-align: center;
      }
      
      .item-name {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 5px;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.3);
      }

      .item-section {
        background-color: var(--card-bg);
        border-radius: 15px;
        padding: 25px;
        margin-bottom: 30px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
      }

      .section-title {
        color: var(--primary-dark);
        border-bottom: 2px solid var(--primary-light);
        padding-bottom: 10px;
        margin-bottom: 20px;
        font-weight: 600;
      }

      .item-description {
        font-size: 1.1rem;
        line-height: 1.6;
        color: #555;
        border-left: 4px solid var(--primary-light);
        padding-left: 15px;
        margin: 20px 0;
      }

      .info-icon {
        color: var(--primary-dark);
        margin-right: 8px;
      }

      .item-sidebar {
        background-color: var(--card-bg);
        border-radius: 15px;
        padding: 20px;
        position: sticky;
        top: 20px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
      }

      .back-link {
        display: flex;
        align-items: center;
        color: var(--primary-dark);
        text-decoration: none;
        margin-bottom: 15px;
        font-weight: 500;
      }

      .back-link:hover {
        color: var(--accent-color);
      }

      .portion-card {
        background-color: #fff;
        border-radius: 12px;
        padding: 15px;
        margin-bottom: 15px;
        box-shadow: 0 3px 10px rgba(0, 0, 0, 0.05);
        border-left: 4px solid var(--primary-color);
        transition: all 0.3s ease;
      }

      .portion-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 5px 15px rgba(255, 127, 80, 0.15);
      }

      .portion-name {
        font-weight: 600;
        font-size: 1.1rem;
        color: var(--text-color);
      }

      .portion-price {
        color: var(--primary-dark);
        font-weight: 700;
        font-size: 1.2rem;
      }

      .nutritional-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 15px;
      }

      .nutritional-item {
        background-color: #fff;
        border-radius: 10px;
        padding: 15px;
        box-shadow: 0 3px 10px rgba(0, 0, 0, 0.05);
        text-align: center;
        transition: all 0.3s ease;
      }

      .nutritional-item:hover {
        transform: translateY(-3px);
        box-shadow: 0 5px 15px rgba(255, 127, 80, 0.15);
      }

      .nutritional-label {
        font-size: 0.9rem;
        color: #666;
        margin-bottom: 5px;
      }

      .nutritional-value {
        font-size: 1.4rem;
        font-weight: 700;
        color: var(--primary-dark);
      }

      /* Estilos para os cards de imagem */
      .images-container {
        display: flex;
        flex-wrap: wrap;
        gap: 15px;
        justify-content: center;
        padding: 20px;
        background-color: #fff;
        border-radius: 0 0 15px 15px;
      }

      .image-card {
        width: calc(33.33% - 10px);
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
        cursor: pointer;
      }

      .image-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 15px rgba(255, 127, 80, 0.2);
      }

      .image-card img {
        width: 100%;
        height: 200px;
        object-fit: cover;
        display: block;
      }
      
      .category-badge {
        background-color: var(--primary-color);
        color: white;
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 0.9rem;
        font-weight: 600;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        display: inline-block;
        margin: 5px 0;
      }
      
      @media (max-width: 992px) {
        .nutritional-grid {
          grid-template-columns: repeat(2, 1fr);
        }
        
        .image-card {
          width: calc(50% - 15px);
        }
      }

      @media (max-width: 768px) {
        .item-name {
          font-size: 2rem;
        }
        
        .nutritional-grid {
          grid-template-columns: repeat(2, 1fr);
        }
        
        .image-card {
          width: calc(50% - 10px);
        }
        
        .image-card img {
          height: 150px;
        }
      }

      @media (max-width: 576px) {
        .nutritional-grid {
          grid-template-columns: 1fr;
        }
        
        .image-card {
          width: 100%;
        }
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container">
        <a class="navbar-brand" href="/">FlavorFlow</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if (locals.user) { %>
              <li class="nav-item me-2">
                <a class="nav-link" href="/api/v1/auth/profile">
                  <button class="btn text-white">
                    <i class="bi bi-person-circle me-1"></i> My Account
                  </button>
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/api/v1/cart/cartPage">
                  <button class="btn text-white">
                    <i class="bi bi-cart3 me-1"></i> Cart
                  </button>
                </a>
              </li>
            <% } else { %>
              <li class="nav-item me-2">
                <a class="nav-link" href="/api/v1/auth/login">
                  <button class="btn text-white">
                    <i class="bi bi-box-arrow-in-right me-1"></i> Login
                  </button>
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/api/v1/auth/signup">
                  <button class="btn text-white">
                    <i class="bi bi-person-plus me-1"></i> Sign Up
                  </button>
                </a>
              </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <div class="item-header">
        <!-- Item info header -->
        <div class="item-info-header">
          <h1 class="item-name"><%= menuItem.name %></h1>
        </div>
        
        <!-- Cards de Imagens -->
        <% if (menuItem.images && menuItem.images.length > 0) { %>
          <div class="images-container pb-5">
            <% menuItem.images.forEach((image, index) => { %>
              <div class="image-card">
                <img src="/<%= image %>" alt="<%= menuItem.name %>">
              </div>
            <% }); %>
          </div>
          <% } else { %>
            <div class="images-container">
              <div class="image-card">
                <img src="/images/default-menu.png" alt="Default Image">
              </div>
            </div>
        <% } %>

      <div class="row">
        <div class="col-lg-8 pt-4">
          <div class="item-section">
            <h2 class="section-title"><i class="bi bi-info-circle-fill info-icon"></i>About this item</h2>
            <p class="item-description"><%= menuItem.description %></p>
          </div>
          
          <div class="item-section">
            <h2 class="section-title"><i class="bi bi-star info-icon"></i>Nutritional Information</h2>
            <div class="nutritional-grid">
              <div class="nutritional-item">
                <div class="nutritional-label">Calories</div>
                <div class="nutritional-value"><%= menuItem.nutritionalInfo.calories %> kcal</div>
              </div>
              <div class="nutritional-item">
                <div class="nutritional-label">Proteins</div>
                <div class="nutritional-value"><%= menuItem.nutritionalInfo.proteins %>g</div>
              </div>
              <div class="nutritional-item">
                <div class="nutritional-label">Carbohydrates</div>
                <div class="nutritional-value"><%= menuItem.nutritionalInfo.carbs %>g</div>
              </div>
              <div class="nutritional-item">
                <div class="nutritional-label">Fats</div>
                <div class="nutritional-value"><%= menuItem.nutritionalInfo.fats %>g</div>
              </div>
            </div>
          </div>
          
          <div class="item-section">
            <h2 class="section-title"><i class="bi bi-aspect-ratio info-icon"></i>Portions & Prices</h2>
            <% if (menuItem.portions && menuItem.portions.length > 0) { %>
              <% menuItem.portions.forEach(portion => { %>
                <div class="portion-card">
                  <div class="d-flex justify-content-between align-items-center">
                    <span class="portion-name"><%= portion.name %></span>
                    <span class="portion-price">€ <%= portion.price.toFixed(2) %></span>
                  </div>
                  <div class="mt-3">
                    <form action="/api/v1/cart/addItem" method="POST">
                      <input type="text" class="d-none" name="menuItem" value="<%= menuItem._id %>">
                      <input type="text" class="d-none" name="portionName" value="<%= portion.name %>">
                      <input type="number" class="d-none" name="portionPrice" value="<%= portion.price %>">
                      <input type="number" class="d-none" name="quantity" value="1">
                      <button type="submit" class="btn btn-primary" onclick="return confirm('You sure you want to add this item to the cart?')">
                        Add to cart
                      </button>                    
                    </form>
                  </div>
                </div>
              <% }); %>
            <% } else { %>
              <div class="alert alert-warning">
                <i class="bi bi-exclamation-circle me-2"></i>No portion information available.
              </div>
            <% } %>
          </div>
        </div>
        
        <div class="col-lg-4 pt-4">
          <div class="item-sidebar">
            <a href="javascript:history.back()" class="back-link">
              <i class="bi bi-arrow-left me-2"></i>Back to menu
            </a>
            
            <div class="mt-4">
              <h5><i class="bi bi-tag info-icon"></i>Category</h5>
              <div class="category-badge"><%= menuItem.category.name %></div>
            </div>
            
            <hr class="my-4">
            
            <% if (menuItem.restaurant) { %>
            <div>
              <h5><i class="bi bi-shop info-icon"></i>Restaurant</h5>
              <a href="/api/v1/restaurants/restaurant/<%= menuItem.restaurant.id %>" class="btn btn-primary mt-2 w-100">
                Visit <%= menuItem.restaurant.name %>
              </a>
            </div>
            <% } %>
            
            <hr class="my-4">
            
            <div>
              <h5><i class="bi bi-clock-history info-icon"></i>Added on</h5>
              <p><%= new Date(menuItem.createdAt).toLocaleDateString() %></p>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>