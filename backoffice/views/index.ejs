<!DOCTYPE html>
<html lang="pt">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>FlavorFlow - Incredible Flavors</title>
    <link rel="icon" type="image/x-icon" href="/images/icon/icon.png">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.0/font/bootstrap-icons.css">
    <style>
        .hero-section {
            /* padding-top: 10px; */
            text-align: center;
            position: relative;
            overflow: hidden;
            margin-bottom: 80px;
        }

        .hero-section img {
            width: 100%;
            max-height: 60vh;
            object-fit: contain;
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
        }

        .hero-section .container {
            position: relative;
            z-index: 2;
            padding-top: 50vh;
        }

        .hero-section h1 {
            margin-bottom: 20px;
            color: var(--primary-dark);
            font-weight: 700;
        }

        .hero-section p {
            margin-bottom: 30px;
            font-size: 1.2rem;
        }

        .btn-explore {
            background: linear-gradient(to right, var(--primary-color), var(--primary-dark));
            border: none;
            padding: 12px 30px;
            border-radius: 30px;
            font-weight: 600;
            color: white;
            letter-spacing: 0.5px;
            box-shadow: 0 4px 15px rgba(255, 69, 0, 0.3);
            transition: all 0.3s ease;
            font-size: 1.1rem;
        }

        .btn-explore:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(255, 69, 0, 0.4);
            color: white;
        }

        .search-bar {
            display: flex;
            justify-content: center;
            gap: 8px;
            margin-top: 10px;
            flex-wrap: wrap;
        }

        #how-it-works {
            padding: 60px 0;
            margin-top: 120px;
        }

        #how-it-works h2 {
            color: var(--primary-dark);
            font-weight: 700;
            margin-bottom: 40px;
        }

        #how-it-works .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            height: 100%;
        }

        #how-it-works .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        }

        #how-it-works .card-body {
            padding: 30px;
            text-align: center;
        }

        #how-it-works .card-body i {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 15px;
            display: block;
        }

        #how-it-works .card-title {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 15px;
            color: var(--primary-dark);
        }

        #how-it-works .card-text {
            color: #666;
            font-size: 1rem;
        }

        #restaurantes {
            padding: 70px 0;
            background-color: #fff;
        }

        #restaurantes h2 {
            color: var(--primary-dark);
            font-weight: 700;
            margin-bottom: 40px;
        }

        .restaurant-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            margin-bottom: 30px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .restaurant-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        }

        .restaurant-card img {
            height: 200px;
            object-fit: cover;
        }

        .restaurant-card .card-body {
            padding: 25px;
        }

        .restaurant-card .card-title {
            font-weight: 600;
            margin-bottom: 12px;
            color: var(--primary-dark);
        }

        .restaurant-card .card-text {
            color: #555;
            margin-bottom: 15px;
        }

        .btn-order {
            background: linear-gradient(to right, var(--primary-color), var(--primary-dark));
            border: none;
            padding: 8px 20px;
            border-radius: 30px;
            font-weight: 600;
            color: white;
            letter-spacing: 0.5px;
            box-shadow: 0 4px 10px rgba(255, 69, 0, 0.2);
            transition: all 0.3s ease;
        }

        .btn-order:hover {
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(255, 69, 0, 0.3);
        }

        @media (max-width: 768px) {
            .search-bar {
                flex-direction: column;
                align-items: center;
            }

            .btn-explore {
                width: 100%;
                max-width: 250px;
            }

            #hero-title {
                padding-top: 50px;
            }
            #how-it-works {
                margin-top: 60px;
            }
            
            #how-it-works .card {
                margin-bottom: 20px;
            }
            
            .hero-section .container {
                padding-top: 40vh;
            }
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">FlavorFlow</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <% if (locals.user) { %>
                        <li class="nav-item me-2">
                            <a class="nav-link" href="/api/v1/auth/profile">
                                <button class="btn text-white">
                                    <i class="bi bi-person-circle me-1"></i> My Account
                                </button>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/api/v1/cart/cartPage">
                              <button class="btn text-white">
                                <i class="bi bi-cart3 me-1"></i> Cart
                              </button>
                            </a>
                        </li>
                    <% } else { %>
                        <li class="nav-item me-2">
                            <a class="nav-link" href="/api/v1/auth/login">
                                <button class="btn text-white">
                                    <i class="bi bi-box-arrow-in-right me-1"></i> Login
                                </button>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/api/v1/auth/signup">
                                <button class="btn text-white">
                                    <i class="bi bi-person-plus me-1"></i> Sign Up
                                </button>
                            </a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <header class="hero-section">
        <div class="container text-center">
            <img src="/images/heroImage.png" alt="Delicious Food" class="img-fluid" />
            <h1 id="hero-title" class="display-4 mt-5">Discover Incredible Flavors</h1>
            <p class="lead">Order meals from the best restaurants in your city!</p>
            <div class="search-bar">
                <a href="#restaurantes" class="btn btn-explore">Explore Restaurants</a>
                <a href="/api/v1/menus/searchPage" class="btn btn-explore">Quick Search <i class="bi bi-search ms-2"></i></a>
            </div>
        </div>
    </header>

    <section id="how-it-works" class="py-5">
        <div class="container">
            <h2 class="text-center mb-5">How It Works</h2>
            <div class="row">
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <i class="bi bi-shop"></i>
                            <h5 class="card-title">Find a Restaurant</h5>
                            <p class="card-text">Browse our curated selection of local restaurants with verified quality and service.</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <i class="bi bi-menu-button"></i>
                            <h5 class="card-title">Choose Your Meal</h5>
                            <p class="card-text">Explore menus and select your favorite dishes from a wide variety of options.</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <i class="bi bi-truck"></i>
                            <h5 class="card-title">Get It Delivered</h5>
                            <p class="card-text">Place your order and receive it at your doorstep - hot, fresh and on time.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="restaurantes" class="py-5">
        <div class="container">
            <h2 class="text-center mb-5">Featured Restaurants</h2>
            <div class="row" id="restaurantList">
                <% if (restaurants && restaurants.length > 0) { %>
                    <% restaurants.forEach(restaurant => { %>
                        <div class="col-md-4 mb-4">
                            <div class="card restaurant-card">
                                <img src="<%= restaurant.image || '/images/default-restaurant.png' %>" class="card-img-top" alt="<%= restaurant.name %>">
                                <div class="card-body">
                                    <h5 class="card-title"><%= restaurant.name %></h5>
                                    <p class="card-text"><%= restaurant.description %></p>
                                    <p class="card-text">
                                        <small class="text-muted">
                                            <i class="bi bi-geo-alt me-1"></i> <%= restaurant.address.street %>, <%= restaurant.address.city %> - <%= restaurant.address.postalCode %>
                                        </small>
                                    </p>                      
                                    <a href="/api/v1/restaurants/restaurant/<%= restaurant._id %>" class="btn btn-order">Order Now <i class="bi bi-arrow-right ms-1"></i></a>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="col-12 text-center">
                        <div class="alert alert-dark">
                            <i class="bi bi-info-circle me-2"></i> There are no restaurants available at the moment. Please check back later!
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
    </section>

    <footer class="bg-dark text-white py-3">
        <div class="container text-center">
            <p>&copy; 2025 FlavorFlow. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>    
</body>
</html>