<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Cart - FlavorFlow</title>
    <link rel="icon" type="image/x-icon" href="/images/icon/icon.png">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.0/font/bootstrap-icons.css"
    />
    <style>
      .btn-primary {
        background-color: var(--primary-dark);
        border-color: var(--primary-dark);
        padding: 8px 20px;
        border-radius: 25px;
        font-weight: 600;
        transition: all 0.3s ease;
      }

      .btn-primary:hover {
        background-color: var(--accent-color);
        border-color: var(--accent-color);
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(255, 69, 0, 0.2);
      }
      
      .btn-outline-danger {
        color: #dc3545;
        border-color: #dc3545;
        border-radius: 25px;
        padding: 8px 20px;
        transition: all 0.3s ease;
      }
      
      .btn-outline-danger:hover {
        background-color: #dc3545;
        color: white;
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(220, 53, 69, 0.2);
      }

      .cart-header {
        background-color: var(--primary-color);
        color: white;
        padding: 30px 20px;
        border-radius: 15px;
        margin-bottom: 30px;
        text-align: center;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }
      
      .cart-title {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 10px;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.3);
      }
      
      .cart-subtitle {
        font-size: 1.2rem;
        opacity: 0.9;
      }

      .cart-section {
        background-color: var(--card-bg);
        border-radius: 15px;
        padding: 25px;
        margin-bottom: 30px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
      }

      .section-title {
        color: var(--primary-dark);
        border-bottom: 2px solid var(--primary-light);
        padding-bottom: 10px;
        margin-bottom: 20px;
        font-weight: 600;
      }

      .cart-item {
        background-color: #fff;
        border-radius: 12px;
        padding: 20px;
        margin-bottom: 15px;
        box-shadow: 0 3px 10px rgba(0, 0, 0, 0.05);
        border-left: 4px solid var(--primary-color);
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: space-between;
      }

      .cart-item:hover {
        transform: translateY(-3px);
        box-shadow: 0 5px 15px rgba(255, 127, 80, 0.15);
      }
      
      .item-image {
        width: 100px;
        height: 100px;
        border-radius: 10px;
        object-fit: cover;
        margin-right: 20px;
        box-shadow: 0 3px 6px rgba(0, 0, 0, 0.1);
      }
      
      .item-details {
        flex-grow: 1;
      }
      
      .item-name {
        font-weight: 700;
        font-size: 1.2rem;
        color: var(--text-color);
        margin-bottom: 5px;
      }
      
      .item-portion {
        color: #666;
        font-size: 0.95rem;
        margin-bottom: 8px;
      }
      
      .item-price {
        color: var(--primary-dark);
        font-weight: 700;
        font-size: 1.2rem;
      }
      
      .item-controls {
        display: flex;
        align-items: center;
        gap: 15px;
      }
      
      .quantity-control {
        display: flex;
        align-items: center;
        background-color: #f8f9fa;
        border-radius: 25px;
        padding: 5px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
      }
      
      .quantity-btn {
        background-color: var(--primary-light);
        color: white;
        border: none;
        width: 30px;
        height: 30px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.2s ease;
      }
      
      .quantity-btn:hover {
        background-color: var(--primary-dark);
        transform: scale(1.1);
      }
      
      .quantity-input {
        width: 50px;
        text-align: center;
        border: none;
        background: transparent;
        font-weight: 600;
        color: var(--text-color);
      }
      
      .remove-btn {
        color: #dc3545;
        background: none;
        border: none;
        font-size: 1.2rem;
        cursor: pointer;
        transition: all 0.2s ease;
      }
      
      .remove-btn:hover {
        transform: scale(1.2);
      }
      
      .cart-summary {
        background-color: var(--card-bg);
        border-radius: 15px;
        padding: 25px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        position: sticky;
        top: 20px;
      }
      
      .summary-item {
        display: flex;
        justify-content: space-between;
        padding: 10px 0;
        border-bottom: 1px dashed #eee;
      }
      
      .summary-label {
        color: #666;
      }
      
      .summary-value {
        font-weight: 600;
        color: var(--text-color);
      }
      
      .summary-total {
        display: flex;
        justify-content: space-between;
        padding: 15px 0;
        margin-top: 10px;
        border-top: 2px solid var(--primary-light);
      }
      
      .total-label {
        font-size: 1.2rem;
        font-weight: 700;
      }
      
      .total-value {
        font-size: 1.4rem;
        font-weight: 800;
        color: var(--primary-dark);
      }
      
      .checkout-btn {
        width: 100%;
        padding: 12px;
        margin-top: 20px;
        font-size: 1.1rem;
        letter-spacing: 0.5px;
      }
      
      .empty-cart {
        text-align: center;
        padding: 50px 20px;
      }
      
      .empty-cart-icon {
        font-size: 4rem;
        color: #ccc;
        margin-bottom: 20px;
      }
      
      .empty-cart-message {
        font-size: 1.5rem;
        color: #666;
        margin-bottom: 30px;
      }
      
      @media (max-width: 992px) {
        .cart-item {
          flex-direction: column;
          align-items: flex-start;
        }
        
        .item-image {
          margin-right: 0;
          margin-bottom: 15px;
          width: 100%;
          height: 150px;
        }
        
        .item-controls {
          width: 100%;
          justify-content: space-between;
          margin-top: 15px;
        }
      }

      @media (max-width: 768px) {
        .cart-title {
          font-size: 2rem;
        }
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container">
        <a class="navbar-brand" href="/">FlavorFlow</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if (locals.user) { %>
              <li class="nav-item me-2">
                <a class="nav-link" href="/api/v1/auth/profile">
                  <button class="btn text-white">
                    <i class="bi bi-person-circle me-1"></i> My Account
                  </button>
                </a>
              </li>
            <% } else { %>
              <li class="nav-item me-2">
                <a class="nav-link" href="/api/v1/auth/login">
                  <button class="btn text-white">
                    <i class="bi bi-box-arrow-in-right me-1"></i> Login
                  </button>
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/api/v1/auth/signup">
                  <button class="btn text-white">
                    <i class="bi bi-person-plus me-1"></i> Sign Up
                  </button>
                </a>
              </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <div class="cart-header">
        <h1 class="cart-title"><i class="bi bi-cart3 me-2"></i>Your Cart</h1>
        <p class="cart-subtitle">Review your selected items before checkout</p>
      </div>
      
      <div class="row">
        <div class="col-lg-8">
          <div class="cart-section">
            <h2 class="section-title"><i class="bi bi-bag-check me-2"></i>Cart Items</h2>
            
            <% if (cartItems && cartItems.length > 0) { %>
              <div id="cart-items-container">
                <% cartItems.forEach(item => { %>
                  <div class="cart-item" data-menu-id="<%= item.menuItem %>" data-portion-name="<%= item.portionName %>">
                    <% if (item.menuItem.images && item.menuItem.images.length > 0) { %>
                        <img src="/<%= item.menuItem.images[0] %>" alt="<%= item.menuItem.name %>" class="item-image">
                      <% } else { %>
                        <img src="/images/default-menu.png" alt="<%= item.menuItem.name %>" class="item-image">
                      <% } %>
                    <div class="item-details">
                      <h3 class="item-name"><%= item.menuItem.name  %></h3>
                      <p class="item-portion">Portion: <%= item.portionName %></p>
                      <div class="item-price">€ <%= item.portionPrice.toFixed(2) %></div>
                    </div>
                    <div class="item-controls">
                        <div class="quantity-control">
                            <form action="/api/v1/cart/updateItemQuantity/<%= item.menuItem._id %>/<%= item.portionName %>" method="POST" style="display: inline;">
                                <input type="number" class="d-none" name="quantity" value="<%= item.quantity - 1 %>">
                                <button type="submit" class="quantity-btn decrease-btn" <%= item.quantity <= 1 ? 'disabled' : '' %>>
                                    <i class="bi bi-dash"></i>
                                </button>
                            </form>
                        
                            <input type="number" class="quantity-input" value="<%= item.quantity %>" min="1" max="99" readonly>
                            
                            <form action="/api/v1/cart/updateItemQuantity/<%= item.menuItem._id %>/<%= item.portionName %>" method="POST" style="display: inline;">
                                <input type="number" class="d-none" name="quantity" value="<%= item.quantity + 1 %>">
                                <button type="submit" class="quantity-btn increase-btn">
                                    <i class="bi bi-plus"></i>
                                </button>
                            </form>
                        </div>

                    <form action="/api/v1/cart/removeItem/<%= item.menuItem._id %>/<%= item.portionName %>" method="POST" style="display: inline;">
                        <button type="submit" class="btn btn-outline-danger" onclick="return confirm('You sure you do remove this item from the cart?')">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                    </div>
                  </div>
                <% }); %>
              </div>
              
              <div class="d-flex justify-content-between mt-4">
                <a href="/" class="btn btn-outline-secondary">
                  <i class="bi bi-arrow-left me-2"></i>Continue Shopping
                </a>
                <form action="/api/v1/cart/clearCart" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-outline-danger" onclick="return confirm('You sure you want the cart?')">
                        <i class="bi bi-trash me-2"></i>Clear Cart
                    </button>
                </form>
              </div>
            <% } else { %>
              <div class="empty-cart">
                <div class="empty-cart-icon">
                  <i class="bi bi-cart-x"></i>
                </div>
                <h3 class="empty-cart-message">Your cart is empty</h3>
                <a href="/" class="btn btn-primary">
                  <i class="bi bi-shop me-2"></i>Browse Menu
                </a>
              </div>
            <% } %>
          </div>
        </div>
        
        <div class="col-lg-4">
          <div class="cart-summary">
            <h2 class="section-title"><i class="bi bi-receipt me-2"></i>Order Summary</h2>
            
            <% if (cartItems && cartItems.length > 0) { %>
              <div id="summary-details">
                <div class="summary-item">
                  <span class="summary-label">Items (<span id="items-count"><%= cartItems.length %></span>)</span>
                  <span class="summary-value" id="items-subtotal">€ 0.00</span>
                </div>
                
                <div class="summary-total">
                  <span class="total-label">Total</span>
                  <span class="total-value" id="total-price">€ 0.00</span>
                </div>
                
                <button class="btn btn-primary checkout-btn">
                  <i class="bi bi-credit-card me-2"></i>Proceed to Checkout
                </button>
              </div>
            <% } else { %>
              <div class="text-center p-4">
                <i class="bi bi-bag-x" style="font-size: 2rem; color: #ccc;"></i>
                <p class="mt-3 text-muted">Add items to your cart to see the order summary</p>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
        let subtotal = 0;
        
        const cartItems = document.querySelectorAll('.cart-item');
        cartItems.forEach(item => {
            const priceText = item.querySelector('.item-price').textContent;
            const price = parseFloat(priceText.replace('€ ', ''));
            const quantity = parseInt(item.querySelector('.quantity-input').value);
            subtotal += price * quantity;
        });
        
        // Update displays
        document.getElementById('items-subtotal').textContent = '€ ' + subtotal.toFixed(2);
        document.getElementById('total-price').textContent = '€ ' + subtotal.toFixed(2);
        });
  </script>
  </body>
</html>