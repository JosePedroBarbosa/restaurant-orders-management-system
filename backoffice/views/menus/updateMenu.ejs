<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Menu - <%= menu.name %> - FlavorFlow</title>
    <link rel="icon" type="image/x-icon" href="/images/icon/icon.png">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.0/font/bootstrap-icons.css">
    <style>
      .btn-primary {
        background-color: var(--primary-dark);
        border-color: var(--primary-dark);
        padding: 8px 20px;
        border-radius: 25px;
        font-weight: 600;
        transition: all 0.3s ease;
      }

      .btn-primary:hover {
        background-color: var(--accent-color);
        border-color: var(--accent-color);
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(255, 69, 0, 0.2);
      }
      
      .menu-header {
        position: relative;
        height: 200px;
        overflow: hidden;
        border-radius: 15px;
        margin-bottom: 30px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }

      .menu-image-preview {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
      
      .menu-overlay {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
        padding: 30px 20px;
        color: white;
      }
      
      .menu-title {
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 5px;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.5);
      }

      .form-section {
        background-color: var(--card-bg);
        border-radius: 15px;
        padding: 25px;
        margin-bottom: 30px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
      }

      .section-title {
        color: var(--primary-dark);
        border-bottom: 2px solid var(--primary-light);
        padding-bottom: 10px;
        margin-bottom: 20px;
        font-weight: 600;
      }

      .info-icon {
        color: var(--primary-dark);
        margin-right: 8px;
      }

      .form-label {
        font-weight: 500;
        color: #444;
      }

      .form-control:focus {
        border-color: var(--primary-light);
        box-shadow: 0 0 0 0.25rem rgba(255, 99, 71, 0.25);
      }

      .table-responsive {
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
      }

      .table thead {
        background-color: var(--primary-dark);
        color: white;
      }

      .btn-action {
        border-radius: 20px;
        padding: 5px 15px;
        font-weight: 500;
        font-size: 0.85rem;
        transition: all 0.3s ease;
      }

      .btn-edit {
        background-color: #FFC107;
        border-color: #FFC107;
        color: #212529;
      }

      .btn-edit:hover {
        background-color: #E0A800;
        border-color: #E0A800;
        transform: translateY(-2px);
        box-shadow: 0 3px 6px rgba(224, 168, 0, 0.2);
      }

      .btn-remove {
        background-color: #DC3545;
        border-color: #DC3545;
        color: white;
      }

      .btn-remove:hover {
        background-color: #C82333;
        border-color: #C82333;
        transform: translateY(-2px);
        box-shadow: 0 3px 6px rgba(200, 35, 51, 0.2);
      }
      
      .btn-back {
        display: flex;
        align-items: center;
        color: var(--primary-dark);
        text-decoration: none;
        font-weight: 500;
        transition: all 0.3s ease;
      }

      .btn-back:hover {
        color: var(--accent-color);
        transform: translateX(-3px);
      }
      
      .btn-save {
        background-color: var(--primary-dark);
        border-color: var(--primary-dark);
        padding: 8px 25px;
        border-radius: 25px;
        color: white;
        font-weight: 600;
      }

      .btn-save:hover {
        background-color: var(--accent-color);
        border-color: var(--accent-color);
        transform: translateY(-2px);
        color: white;
        box-shadow: 0 4px 8px rgba(255, 69, 0, 0.2);
      }

      .img-preview-container {
        margin-top: 10px;
        position: relative;
        width: 100%;
        max-width: 200px;
        overflow: hidden;
        border-radius: 10px;
        box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
      }

      .img-preview {
        width: 100%;
        height: auto;
        display: block;
      }

      .image-overlay {
        position: absolute;
        top: 5px;
        right: 5px;
        background-color: rgba(0, 0, 0, 0.5);
        color: white;
        width: 30px;
        height: 30px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        opacity: 0;
        transition: opacity 0.3s ease;
      }

      .img-preview-container:hover .image-overlay {
        opacity: 1;
      }

      @media (max-width: 768px) {
        .menu-header {
          height: 150px;
        }
        
        .menu-title {
          font-size: 1.5rem;
        }
        
        .form-section {
          padding: 15px;
        }
      }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container">
        <a class="navbar-brand" href="/">FlavorFlow</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item me-2">
              <a class="nav-link" href="/api/v1/restaurants/updateRestaurant">
                <button class="btn text-white">
                  Restaurant Dashboard <i class="bi bi-speedometer2 ms-1"></i>
                </button>
              </a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container my-5">
        <div class="menu-header">
            <% if (menu.image) { %>
                <img src="/<%= menu.image %>" class="menu-image-preview" alt="<%= menu.name %>">
            <% } else { %>
              <img src="/images/default-menu.png" class="menu-image-preview" alt="<%= menu.name %>">
            <% } %>
            <div class="menu-overlay">
              <h1 class="menu-title">Edit: <%= menu.name %></h1>
              <div class="d-flex align-items-center">
                <i class="bi bi-tag info-icon"></i>
                <span><%= menu.menuItems ? menu.menuItems.length : 0 %> items available</span>
              </div>
            </div>
        </div>

        <form action="/api/v1/menus/updateMenu/<%= menu._id %>" method="POST" enctype="multipart/form-data">
            <div class="form-section">
                <h2 class="section-title"><i class="bi bi-info-circle-fill info-icon"></i>Current Menu Information</h2>
                <div class="row g-4">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label for="name" class="form-label">Menu Name:</label>
                            <input type="text" class="form-control" id="name" name="name" value="<%= menu.name %>" required minlength="3">
                            <div class="form-text">At least 3 characters</div>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label for="image" class="form-label">Menu Image:</label>
                            <input type="file" class="form-control" id="image" name="image" accept="image/*">
                            <% if (menu.image) { %>
                                <div class="img-preview-container mt-2">
                                    <img src="/<%= menu.image %>" alt="Current menu image" class="img-preview">
                                    <div class="image-overlay" title="Change image">
                                        <i class="bi bi-pencil"></i>
                                    </div>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>

                <div class="mb-3">
                    <label for="description" class="form-label">Description:</label>
                    <textarea class="form-control" id="description" name="description" rows="3" minlength="10" maxlength="255"><%= menu.description || '' %></textarea>
                    <div class="form-text">Between 10 and 255 characters</div>
                </div>
                <button type="submit" class="btn btn-save mt-4">
                  <i class="bi bi-check2-circle me-2"></i>Save Changes
                </button>
            </div>
      </form>

        <div class="form-section mt-5">
          <h2 class="section-title"><i class="bi bi-list-ul info-icon"></i>Manage Menu Items</h2>
          <div class="mb-4">
              <div class="table-responsive">
                  <table class="table table-hover">
                      <thead>
                          <tr>
                              <th>Name</th>
                              <th>Category</th>
                              <th>Description</th>
                              <th class="text-center">Actions</th>
                          </tr>
                      </thead>
                      <tbody id="menuItemsTable">
                          <% if (menu.menuItems && menu.menuItems.length > 0) { %>
                              <% menu.menuItems.forEach(function(item, index) { %>
                                  <tr>
                                      <td><%= item.name %></td>
                                      <td><%= item.category.name %></td>
                                      <td><%= item.description %></td>
                                      <td class="text-center">
                                        <form action="/api/v1/menus/<%= menu._id %>/removeItem/<%= item._id %>" method="POST" style="display: inline;">
                                          <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('You sure you want to delete this menu item?')">
                                            <i class="bi bi-trash me-1"></i> Remove
                                          </button>
                                        </form>
                                      </td>
                                  </tr>
                              <% }); %>
                          <% } else { %>
                              <tr id="noItemsRow">
                                  <td colspan="4" class="text-center py-4">
                                      <i class="bi bi-exclamation-circle me-2 text-warning"></i>
                                      This menu has no items yet. Add some items to enhance your menu!
                                  </td>
                              </tr>
                          <% } %>
                      </tbody>
                  </table>

                  <form action="/api/v1/menus/<%= menu._id %>/addItem/" method="POST">
                      <h2 class="section-title mt-5"><i class="bi bi-plus"></i>Add Menu Item</h2> 
                      <div class="col-12 mb-3">
                        <label for="menuItemId" class="form-label">Available menu items</label>
                        <select class="form-select" id="menuItemId" name="menuItemId" required>
                            <% items.forEach(function(item) { %>
                                <option value="<%= item._id %>">
                                    <%= item.name %>
                                </option>
                            <% }); %>
                        </select>
                    </div>
                      <button type="submit" class="btn btn-save mt-4">
                        <i class="bi bi-check2-circle me-2"></i>Add Menu Item
                      </button>
                  </form>
              </div>
          </div>
      </div>
      </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>