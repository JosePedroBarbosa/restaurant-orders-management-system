<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= menu.name %> - FlavorFlow</title>
    <link rel="icon" type="image/x-icon" href="/images/icon/icon.png">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.0/font/bootstrap-icons.css"
    />
    <style>
      .btn-primary {
        background-color: var(--primary-dark);
        border-color: var(--primary-dark);
        padding: 8px 20px;
        border-radius: 25px;
        font-weight: 600;
        transition: all 0.3s ease;
      }

      .btn-primary:hover {
        background-color: var(--accent-color);
        border-color: var(--accent-color);
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(255, 69, 0, 0.2);
      }

      .menu-header {
        position: relative;
        height: 300px;
        overflow: hidden;
        border-radius: 15px;
        margin-bottom: 30px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }

      .menu-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
      
      .menu-overlay {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
        padding: 30px 20px;
        color: white;
      }
      
      .menu-name {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 5px;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.5);
      }

      .menu-section {
        background-color: var(--card-bg);
        border-radius: 15px;
        padding: 25px;
        margin-bottom: 30px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
      }

      .section-title {
        color: var(--primary-dark);
        border-bottom: 2px solid var(--primary-light);
        padding-bottom: 10px;
        margin-bottom: 20px;
        font-weight: 600;
      }

      .menu-description {
        font-size: 1.1rem;
        line-height: 1.6;
        color: #555;
        border-left: 4px solid var(--primary-light);
        padding-left: 15px;
        margin: 20px 0;
      }

      .info-icon {
        color: var(--primary-dark);
        margin-right: 8px;
      }

      .menu-items {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 25px;
      }

      .menu-item {
        border-radius: 15px;
        overflow: hidden;
        transition: all 0.3s ease;
        background-color: #fff;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        display: flex;
        flex-direction: column;
        height: 100%;
      }

      .menu-item:hover {
        box-shadow: 0 10px 25px rgba(255, 127, 80, 0.15);
        transform: translateY(-5px);
      }

      .menu-item-image-container {
        height: 180px;
        overflow: hidden;
        position: relative;
      }

      .menu-item-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
      }

      .menu-item:hover .menu-item-image {
        transform: scale(1.05);
      }

      .category-badge {
        position: absolute;
        top: 15px;
        right: 15px;
        background-color: var(--primary-color);
        color: white;
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 600;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }

      .menu-item-description {
        color: #666;
        margin: 10px 0;
        font-size: 0.95rem;
      }

      .menu-item-content {
        padding: 20px;
        display: flex;
        flex-direction: column;
        flex-grow: 1;
      }

      .menu-item-name {
        font-size: 1.3rem;
        font-weight: 600;
        color: var(--text-color);
        margin-bottom: 10px;
      }

      .menu-item-description {
        color: #666;
        font-size: 0.95rem;
        line-height: 1.5;
        margin-bottom: 15px;
        flex-grow: 1;
      }

      .menu-item-footer {
        margin-top: auto;
        text-align: center;
      }

      .view-item-btn {
        display: inline-block;
        background-color: var(--primary-color);
        color: white;
        border-radius: 25px;
        padding: 8px 20px;
        font-weight: 600;
        transition: all 0.3s ease;
        text-decoration: none;
        width: 100%;
      }

      .view-item-btn:hover {
        background-color: var(--primary-dark);
        color: white;
        transform: translateX(5px);
        box-shadow: 0 4px 8px rgba(255, 69, 0, 0.2);
      }

      .menu-sidebar {
        background-color: var(--card-bg);
        border-radius: 15px;
        padding: 20px;
        position: sticky;
        top: 20px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
      }

      .back-link {
        display: flex;
        align-items: center;
        color: var(--primary-dark);
        text-decoration: none;
        margin-bottom: 15px;
        font-weight: 500;
      }

      .back-link:hover {
        color: var(--accent-color);
      }
      
      @media (max-width: 992px) {
        .menu-items {
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
          }
        }

        @media (max-width: 576px) {
          .menu-items {
            grid-template-columns: 1fr;
          }
          
          .menu-item-image-container {
            height: 160px;
          }
      }

      @media (max-width: 768px) {
        .menu-header {
          height: 200px;
        }
        
        .menu-name {
          font-size: 1.8rem;
        }
        
        .menu-items {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container">
        <a class="navbar-brand" href="/">FlavorFlow</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if (locals.user) { %>
              <li class="nav-item me-2">
                <a class="nav-link" href="/api/v1/auth/profile">
                  <button class="btn text-white">
                    <i class="bi bi-person-circle me-1"></i> My Account
                  </button>
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/api/v1/cart/cartPage">
                  <button class="btn text-white">
                    <i class="bi bi-cart3 me-1"></i> Cart
                  </button>
                </a>
            </li>
            <% } else { %>
              <li class="nav-item me-2">
                <a class="nav-link" href="/api/v1/auth/login">
                  <button class="btn text-white">
                    <i class="bi bi-box-arrow-in-right me-1"></i> Login
                  </button>
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/api/v1/auth/signup">
                  <button class="btn text-white">
                    <i class="bi bi-person-plus me-1"></i> Sign Up
                  </button>
                </a>
              </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <div class="menu-header">
        <% if (menu.image) { %>
            <img src="/<%= menu.image %>" class="card-img-top menu-image" alt="<%= menu.name %>">
        <% } else { %>
          <img src="/images/default-menu.png" class="card-img-top menu-image" alt="<%= menu.name %>">
        <% } %>
        <div class="menu-overlay">
          <h1 class="menu-name"><%= menu.name %></h1>
          <div class="d-flex align-items-center">
            <i class="bi bi-tag info-icon"></i>
            <span><%= menu.menuItems ? menu.menuItems.length : 0 %> items available</span>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="col-lg-8">
          <div class="menu-section">
            <h2 class="section-title"><i class="bi bi-info-circle-fill info-icon"></i>About this menu</h2>
            <p class="menu-description"><%= menu.description %></p>
            
            <% if (menu.restaurant) { %>
            <div class="d-flex align-items-center mt-3">
              <i class="bi bi-shop info-icon"></i>
              <span>From: <a href="/api/v1/restaurants/restaurant/<%= menu.restaurant.id %>" class="text-decoration-none text-dark"><%= menu.restaurant.name %></a></span>
            </div>
            <% } %>
          </div>
          
          <% if (menu.menuItems && menu.menuItems.length > 0) { %>
            <div class="menu-section">
              <h2 class="section-title"><i class="bi bi-list-ul info-icon"></i>Available Meals</h2>
              
              <div class="menu-items">
                <% menu.menuItems.forEach(item => { %>
                  <div class="menu-item">
                    <div class="menu-item-image-container">
                      <% if (item.images && item.images.length > 0) { %>
                        <img src="/<%= item.images[0] %>" class="menu-item-image" alt="<%= item.name %>">
                      <% } else { %>
                          <img src="/images/default-menu-item.png" class="menu-item-image" alt="<%= item.name %>">
                      <% } %>
                      <div class="category-badge"><%= item.category.name %></div>
                    </div>
                    <div class="menu-item-content">
                      <h3 class="menu-item-name"><%= item.name %></h3>
                      <div class="menu-item-description"><%= item.description %></div>
                      <div class="menu-item-footer">
                        <a href="/api/v1/menuItems/item/<%= item.id %>" class="view-item-btn">
                          <i class="bi bi-eye me-2"></i>View Details
                        </a>
                      </div>
                    </div>
                  </div>
                <% }) %>
              </div>
            </div>
            <% } else { %>
            <div class="alert alert-warning mt-4">
              <i class="bi bi-exclamation-circle me-2"></i>Currently this menu doesn't have any items.
            </div>
            <% } %>
        
        <div class="col-lg-4">
          <div class="menu-sidebar">
            <a href="javascript:history.back()" class="back-link">
              <i class="bi bi-arrow-left me-2"></i>Back to restaurant
            </a>
          </div>
        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>